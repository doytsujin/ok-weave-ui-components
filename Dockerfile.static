# Dockerfile.static — Serve out/ statically
FROM nginx:alpine

# Copy unified build output to Nginx web root
COPY out/ /usr/share/nginx/html/

# Expose the default HTTP port
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
