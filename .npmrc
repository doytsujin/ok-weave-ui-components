# Avoid installing optional deps you don’t use
optional=false

# Prefer exact versions
save-exact=true

# Don’t send audit reports to npm registry
audit=false

# Avoid progress bar noise in CI logs
progress=false

# Use local cache if any, do not poll npm stats
fund=false

# Silence update checks
update-notifier=false

# Use cache within workspace
cache=/workspace/.npm
